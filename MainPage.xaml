<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="bp360_admin_panel.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                <Button Text="Get places" Clicked="GetPlaces_Clicked" />
                <Button Text="Add place" Clicked="AddPlace_Clicked" />
                <Button Text="Update place" Clicked="UpdatePlace_Clicked"/>
                <Button Text="Delete place" Clicked="DeletePlace_Clicked"/>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
